# 프로덕션 빌드용 환경변수 예시
# 실제 값으로 채워 frontend/.env.production 으로 복사 후 `npm run build` 실행

# Cloud Run 배포 후 출력되는 백엔드 URL (끝에 /api 붙이기)
VITE_API_BASE_URL=https://ai-smart-memo-backend-xxxxxx.a.run.app/api

# 데모는 실제 백엔드를 사용하므로 false
VITE_USE_MOCK=false
